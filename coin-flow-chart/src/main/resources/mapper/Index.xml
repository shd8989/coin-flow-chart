<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crypto.db.mapper.IndexMapper">
    <!-- 
    <select id="selectDataList" parameterType="map" resultType="IndexEntity">
        match(v:person)
        return v.name
        limit 5
    </select>
     -->
    
    <select id="selectDataList" resultType="IndexEntity">
        match(v:person)
        return v.name
        limit 5
    </select>
    
    <insert id="updateCoinAllData" parameterType="List">
        CREATE (v:coin {data: #{data}})
    </insert>
</mapper>